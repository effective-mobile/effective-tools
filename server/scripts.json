{
  "$schema": "https://deno.land/x/denon@2.4.7/schema.json",
  "scripts": {
    "start": {
      "cmd": "deno run --allow-read=../.. --allow-write=../.. --allow-run --unstable --allow-net ./mod.ts",
      "desc": "run server"
    }
  }
}
